# Skill: Create UI Component

## Trigger
When the user asks to create a new UI component.

## Steps
1. Define component name (PascalCase), purpose, and props interface
2. Create in `src/app/components/` with TypeScript and Tailwind
3. Use functional component; destructure props; add accessibility (semantic HTML, aria where needed)
4. Follow design tokens (CSS variables) and responsive layout
