---
description: "Secrets and env handling"
alwaysApply: true
---

# Secrets Handling Rules

- All secrets MUST be in environment variables (e.g. `process.env.NEXT_PUBLIC_SITE_URL`)
- Provide `.env.example` with placeholder keys only; never commit `.env` or real values
- Use different env files per environment; never log secret values
